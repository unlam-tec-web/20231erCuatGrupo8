export interface Product {
    id: string;
    img: string;
    name: string;
    description: string;
    category: string; // Tipo auto, pickup, etc(lo pide en el tp)/clasificación
    price: number;
}

